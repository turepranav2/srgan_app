# SRGAN App

This is a simple Streamlit app that uses a trained SRGAN generator model to perform image super-resolution.

## Instructions

1. Place your `generator_weights.h5` model in the root directory.
2. Install requirements:
```bash
pip install -r requirements.txt